# packages/web 的构建期变量（vite 只读 VITE_ 前缀；复制成 .env.local 本地用，.env* 不入库）。
#
# 后台写入通道（Cloudflare Worker，packages/worker）的地址，不带尾斜杠。
# 留空 / 不设 → /admin 用内存 mock（api/mock.ts），GitHub Pages 今天就是这样；
# 设了 → api/client.ts 的 HttpAdminApi 直连 worker（#68 部署完再配）。
VITE_WORKER_URL=
