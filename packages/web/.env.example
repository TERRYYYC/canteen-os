# packages/web 的构建期变量（vite 只读 VITE_ 前缀；复制成 .env.local 本地用，.env* 不入库）。
#
# 公开的 Worker 根地址，不含令牌、用户名/密码、API 路径、query 或 fragment。
# VITE_ 变量会进入浏览器产物，绝不能放 PAT、角色令牌或其他秘密。
# 留空 / 不设 → Team API 未配置：可读已发布菜单/备料；编辑、保存采购单、发布禁用。
# 不会自动变成可写 mock；显式模拟仅用于测试，不能当成服务已接通。
# 本地联调可用已确认的 http://localhost:<port>；此文件不改变本地 Vite 的配置方式。
# Pages 发布使用仓库 Settings → Secrets and variables → Actions → Variables 的 VITE_WORKER_URL。
# build-deploy.yml 注入该非秘密仓库变量；生产要求公网 HTTPS 根地址，可有一个尾斜杠。
# 只设 github-pages 环境变量或在部署后改值不会改已有产物，必须重新构建。
# 完整可写发布前检查与缺失项：docs/field-test/week-43/ops-checklist.md §0.1。
VITE_WORKER_URL=
