---
feature_ids: []
topics: [team-meals, plan, import, q-ui-t01-02]
doc_kind: author-evidence
created: 2026-09-11
---
# Q-UI-T01-02 author repair checkpoint

Accepted contract: `../Q-UI-T01-02-contract.md` at85be501. Copy's5049 production and independent approval are a separate closed boundary; this repair does not change Purchase. Q's capture-only RED and root's new native RED are archived separately. This directory records D author's actual renderer composition, not a native-browser replay or independent approval.

## Change and ownership

The original C1 document accepts the import as a local edit. A tiny synchronous D capability connection stores only callbacks, scoped to API/mode/auth and the requested known Plan. It returns absent/applied/rejected. Known documents use the original C open/edit; no second session, JSON owner, lock or save operation is created. Missing targets keep the original guarded GET/store/undo path. Rejected or retired known owners retain Import raw and do not fall back to saved source. Applied imports clear the captured old store entry, and synchronous subscriber effects are checked before success/navigation.

Existing merge semantics still target the first matching date/meal/dish and sort stably. A transient object-to-original-position map lets invalid raw follow each original row; explicitly supplied/cleared counts supersede only the matched row's invalid override. Remap happens before C edit emits; refusal restores the previous raw map. Add choices stay pending. No position IDs enter JSON. Import/CSV/parser code is not imported into Plan; the merge function is passed synchronously.

## Observed tests

- `red-composition-first.log`: setup error, not product RED. The author initially expected clean after a valid save; real C correctly reported saved-but-unpublished. All8 affected cases stopped at that setup assertion. The log is preserved.
- `red-composition.log`: genuine product RED on original994a production.1/9 pass,8 fail, including8≠11, explicit unknown reset to8, local deletion restored, raw index misplaced, A/B draft loss, and saving/unknown/conflict imports not applied. `red-composition.test.mjs.txt` is the executed source for that stage.
- `red-composition-expanded.log`:2/13 pass,11 fail, including matching/duplicate semantics.
- `red-calibrated.log`:3/16 pass,13 fail after the accepted raw-priority additions. `red-calibrated.test.mjs.txt` preserves that pre-production test.
- `green-composition-first.log`:16/16 pass after the minimum implementation.
- `green-composition-expanded.log`:28/28 pass with actual-C closed/auth rejection, synchronous edit subscriber route/language/input changes, stale-store clearing, fallback failures/cancellations, unknown recovery and repeated use.
- `green-target.log`:86/86 pass, including30 new cases and all prior Plan/import tests. Two additional cases establish distinct real API boundaries and C refusal restoring the raw remap.
- `type-first.log`: passed local Node20.20.2.

The composition bundles both real page renderers, real C1 API/session/store and explicit mock HTTP envelopes. Its MutationObserver double really executes production detach callbacks. Later boundary tests add only a test-build getter exposing the renderer's original C session, so tests can subscribe or call its real dispose; no C method or controller is replaced. The final additional refusal case supplies a page-remap hook to the real form and invalidates its real C context to verify rollback; it is an adapter boundary probe, alongside29 page-composition cases. This does not claim native clipboard/layout/navigation semantics.

## Early budget check

Original HTTP Vite config was rebuilt before freeze, retaining all precache. `http-first.json` and its logs report Plan59887 gzip (113 bytes below60000), Import51345 and11/11 SW entry closures passing. This early graph is not the final44 proof; final two-config builds and full Web checks follow the code checkpoint. No unrelated optimization or threshold adjustment was made.

The source hashes identify4 production files plus the new test. Original raw log whitespace is preserved. Root owns native green replay against the fixed checkpoint; original nonauthor review is pending.
