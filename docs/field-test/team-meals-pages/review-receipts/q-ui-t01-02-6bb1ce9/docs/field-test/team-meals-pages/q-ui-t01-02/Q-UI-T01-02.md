---
feature_ids: [team-meals]
topics: [acceptance, import, draft-loss, RED]
doc_kind: finding-evidence
created: 2026-09-11
status: RED-owner-repair-required
---

# Q-UI-T01-02：导入另一餐次丢失已有本地编辑

实际 main/pages → C client/editor → B worker.fetch → GitHub FakeRepo；实现仍为 `842b778bd352c0dba8921584ce142d94665e5ca2`。未修改生产模块。调度已独立核实原 T01/T07、SCREEN S08、四份 DOM 和代码链，确认需修并派回原 D。此项不是新增导入设计要求，也不是总验收结论。

## 最小原生复现

1. `/#/admin/plan/team-week`，已保存第一餐 `2026-09-14/lunch/first-dish` 为 8 份，source `6f479b8acdd636a10796640833d0a0aafad267c1`。这是 FakeRepo 模型提交，不是真实 GitHub 历史。
2. 在正式计划页把该餐改成 **11**，不保存。证据 `page-browser/T01-import-unrelated-edit-before.txt`。
3. 点“粘贴导入”，只输入 `2026-09-15 午 第一道样本菜`，解析，再点“导入 1 行，跳过 0 行”。原输入保存在 `T01-import-unrelated-input.txt`。
4. 回到计划页，原 `09-14/lunch` 静默回到 **8**。未显示覆盖提示。`09-15/lunch` 新餐次添加成功，份数为空。见 `T01-import-unrelated-edit-lost.txt/.png` 与 `T01-import-unrelated-all-dates.txt`。操作只有读取；服务端没有新的计划 POST。

更早一次导入同一行也把后续 9 份编辑变为已保存的 8，但本项用**不同日期的新餐次**隔离“是否主动覆盖匹配行”的歧义。这里没有点清空份数，也没有点采用远端或保存。

`page-browser/ledger-T01-import-red-checkpoint.json` 中：请求 39 是最后一次真正 POST，保存 8 份到 `6f479b8a...`；40–43 为之前版本/图片读取。第一次 9→8 的导入期间为请求 44 `GET /catalog` 和 45 `GET /source/plan/team-week`；独立 11→8 只新增请求 46 `GET /source/plan/team-week`。44–46 全部真实 handler 200、相同服务端 revision，源计划均为 8，未新增 POST。after 的 dirty 只说明本地已有导入稿，**不代表11或导入稿已保存**。

## 可执行 RED 与根因

```sh
PATH=/private/tmp/canteen-team-ci-tools/node-v20.20.2-darwin-arm64/bin:$PATH node packages/web/test/e2e/team-meals/import-draft-capture.repro.mjs
```

这是对原生 DOM 捕获执行独立的 `11 == 11` 保留断言，当前实际 after=8、exit 1。**不是重新驱动浏览器，也不冒称新的端到端重放。** 修复后要按上面的动作重新操作原生页面，并传入新证据目录运行同一个断言。旧 RED 原件保留。

`page-browser/T01-import-draft-red.log` 保留 actual 8 / expected 11。首次捕获解析器漏掉 DOM 的 `[active]` 标记导致 undefined，已修正测试解析器并另存 `setup-06-capture-parser.log`，不当产品失败。

调用链证据：`plan-form.ts` 的 `createEditSession` 保存本地编辑；`import.ts` 的 `doImport` 先查 `bindDraftStore(...).getDraftPlan`，查不到该 edit session 的本地稿时 force `api.getPlan` 取得已保存 8；`mergePlan` 后交给 draft store。返回 `plan.ts` 时，`plan-form.load` 对 imported 整份调用 `session.edit`，于是旧 8 覆盖新 11。这是两个草稿持有方之间的交接遗漏，Worker 没有篡改正文。

对应原 T01 真份数保留、T07 页面切换/新编辑不丢稿；导入页本身说明未提供份数会保留已有份数。修复方案由调度交原 owner 决定，Q 不接管生产实现。
