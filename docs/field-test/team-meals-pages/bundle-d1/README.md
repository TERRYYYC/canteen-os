---
feature_ids: [APP-BUNDLE-D1]
topics: [pages, bundle, editor-lifetime]
doc_kind: evidence
created: 2026-09-11
---

# D1 author checkpoint

Frozen input: production at 84f8f3eb19b8ba6c7ef330c7b55890ca690ab185 (fee400/e79 C packages); docs-only input/recovery updates through 5414cf1d16e29ac55a2efbaa8a5f23f313c46267. No unapproved reader consumed. This is an author checkpoint, not independent approval or final composition validation.

Baseline original-config Node 20.20.2 builds reproduced default Dish 84,090 and HTTP Ingredient 60,414 / Dish 85,682 gzip bytes. `baseline-{default,http}.json` enumerate all 11 executed closures with normal SW and no SW separately; all 20 application JS files remain precached. Initial actual-renderer legacy instantiation test failed 0/2 (`initial-api-red.log`), then passed after deferred imports. No fabricated branded/publication handles.

First regrouped builds pass 11/11 each (intermediate only): Ingredient 49,423/49,440; Dish 57,192/57,211; tightest Plan 59,586/59,603 (default/HTTP). Application cache now contains 24 JS chunks, 169,079/169,127 gzip bytes. These are whole rebuilt closure measurements, not subtraction estimates; final composition must be rebuilt again after the approved reader merge. SW/runtime totals are separate in each JSON.

Production split preserves the original draft conversions and image algorithm: `ingredient-draft.ts` has no page/transport dependency; `ingredient-form.ts` is static for Ingredient and an action import for Dish; `editor-image.ts` is imported for photo actions; `editor-actions.ts` defers the original client module and `getApi()` singleton. No-image Dish saves use the original C1 owner without loading legacy API. No shared/config/dependency/precache edits.

Controlled boundary tests wrap each actual D import expression before calling its original import, and use actual renderer/C1/auxiliary registry. They prove synchronous raw/read admission, finish-to-auto-translation handoff without a clean interval or duplicate translation, language/route/buffer/row/auth fences, module failure raw retention without write/unknown, and ordinary loaded translation retry. They do **not** establish native ESM failure recovery. The approved recovery contract states native same-URL module failure cannot retry within the current document: expose failure, retain input, end its read ticket, keep other actions and explicit inline cancel; never reload or discard automatically. Root is preparing the separate actual-main emitted-module browser proof.

`checkpoint-final-target.log`: Node20 95/95 = D1 17 + Dish 28 + Ingredient 31 + original ACK 12 + original unload 7. The original independent 16 ACK sweep is separate and not claimed here. Existing ACK/unknown assertions remain; harness resolution follows extracted D modules, and the old photo/inline tests await the new module boundary before exercising their original pending-decode/write scenario.

Iteration history is retained. `first-typecheck.log` caught a scripted extraction accidentally placing an inline function in an interface; fixed before tests. `first-target.log` records a mistyped test filename. `second-target.log` revealed the old transport test seam no longer intercepted extracted modules; the scope was corrected without weakening assertions. `third/fourth-target.log` record tests reaching pre-module instead of original photo/write stage; new waits preserve the original stages. `owner-first.log` includes a too-strict replacement-buffer probe expectation: a fresh replacement correctly starts its own translation, so it now verifies the translated seed is Replacement, never stale A. `checkpoint-type.log` records an over-pruned Currency type import; restored. No failed run is represented as a product pass.

Final full Web/type, final default/HTTP budgets, native browser artifacts and production hashes follow after the approved reader composition. No overall or independent approval is claimed by this checkpoint.
