# issue #26 完成定义的负向测试夹具。
#
# 这里故意放一个假的 AWS access key ID，用来验证 Secret scan 工作流
# （.github/workflows/secrets-scan.yml）会把含密钥的 PR 判红。
#
# 这个值是随机编出来的，只满足 gitleaks aws-access-token 规则的形状
# （AKIA + 16 位大写字母数字），不对应任何真实 AWS 账号，不授予任何权限。
#
# 本文件所在的分支 test/secrets-scan-negative 在验证完成后会被整个删除——
# gitleaks 扫的是所有 ref，留着会让此后每个 PR 都变红。

AWS_ACCESS_KEY_ID=AKIAZ7QF3MRXK2NDVBWU
