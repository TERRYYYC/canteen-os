# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
*.tsbuildinfo
# scripts/build-data.mjs 产物（三张单 JSON + build.json），CI 每次重建，不入库
packages/web/public/data/*
!packages/web/public/data/.gitkeep
# PWA 图标 PNG 是 packages/web/scripts/gen-icons.mjs 的产物（prebuild 生成）；SVG 入库
packages/web/public/icons/*.png
# packages/web/scripts/gen-qr.mjs 产物（三张单入口二维码 PNG + index.json），build 前重建，不入库
packages/web/public/qr/*
!packages/web/public/qr/.gitkeep

# Env & secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Coverage / cache
coverage/
.cache/
.turbo/

# Video import media（视频原始文件不入库；只在 dish.json 中保留 URL 引用）
skills/**/media/*.mp4
skills/**/media/*.mov

# OFF taxonomy raw dump (ODbL, 2.7MB) — 仅本地参考，不入库
docs/research/v2/ingredients.txt

# 子 thread 交付的 bundle（scripts/land.mjs 落地后移到 landed/），不入库
.handoff/
